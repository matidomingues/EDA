package Ejercicio5;

public class SelectionSort {
	public static void sort(int[] array) {
		int iMin = array[0], pMin = 0, iMax = array[array.length - 1], pMax = array.length;
		for (int i = 0; i < array.length; i++) {
			for (int k = i + 1; k < array.length - (array.length - pMax); k++) {
				if (array[k] == 1) {

				}
			}
		}
	}
}
